# Python
.venv/
venv/
__pycache__/
*.pyc

# Secrets
.env

# Generated raw CSVs (regenerate via data/download_datasets.py)
# db/*.db is intentionally committed so the DB tools work with no setup beyond .env
data/raw/

# OS
.DS_Store

# Personal planning / credentials docs (not for the repo)
PROJECT_PLAN.md
CREDENTIALS_SETUP.md
